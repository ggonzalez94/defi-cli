//! Canonical execution endpoints/contracts/ABIs + default chain RPC map.
//!
//! Mirrors `internal/registry`. ABI fragments are declared via `alloy::sol!`.
//! Used when no `--rpc-url` override is provided.
#![allow(dead_code, unused)]
